[project]
name = "mobilitydcat-validation"
version = "0.1.0"
description = "SHACL validation for mobilityDCAT-AP"
requires-python = ">=3.11"
dependencies = [
    "pyshacl",
    "rdflib",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"